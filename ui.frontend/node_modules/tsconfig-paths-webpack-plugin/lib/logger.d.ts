import { Options } from "tsconfig-paths-webpack-plugin/lib/options";
import { Chalk } from "tsconfig-paths-webpack-plugin/node_modules/chalk";
export declare type LoggerFunc = (message: string) => void;
export interface Logger {
    log: LoggerFunc;
    logInfo: LoggerFunc;
    logWarning: LoggerFunc;
    logError: LoggerFunc;
}
export declare function makeLogger(options: Options, colors: Chalk): Logger;
